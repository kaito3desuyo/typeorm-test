version: '3.7'

services:
  db:
    image: postgres:12
    tty: true
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  nodemodules:
  db-data:
